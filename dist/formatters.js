"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.formatAsTypeScript = formatAsTypeScript;
function formatValue(value) {
  if (typeof value === 'boolean') return String(value);
  if (typeof value === 'number') return String(value);
  // JSON.stringify handles escaping quotes, backslashes, newlines, etc.
  return JSON.stringify(value);
}
function formatEntry(entry, indent) {
  const lines = Object.entries(entry).map(([key, value]) => `${indent}  ${key}: ${formatValue(value)},`);
  return `${indent}{\n${lines.join('\n')}\n${indent}}`;
}
function formatAsTypeScript(data) {
  const entries = data.map(entry => formatEntry(entry, '  ')).join(',\n');
  return `// Auto-generated by npm-license-scraper
export const licenses = [
${entries},
] as const;

export type License = (typeof licenses)[number];
`;
}